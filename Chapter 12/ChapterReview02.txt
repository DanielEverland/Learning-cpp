1. It must be deleted manually, so implement a destructor that does so
2. The default copy constructor only does a shallow copy. Implement an overload which does a deep copy
3. Assignment operator doesn't destroy the member, it simply overrides it. Implement an overload that destroys prior to copying