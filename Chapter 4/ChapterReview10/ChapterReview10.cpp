// ChapterReview10.cpp -- Use enum to define a type called response with the possible values Yes, No and Maybe.
// Yes should be 1, No should be 0, and Maybe should be 2.

#include "pch.h"
#include <iostream>

enum Response
{
	No = 0,
	Yes = 1,
	Maybe = 2,
};